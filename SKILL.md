---
name: complexity-analysis
description: 复杂度分析框架。当用户说"/complexity"、"分析复杂度"、"复杂度分析"、"变更放大"、"认知负荷"、"未知的未知"、"评估这个功能的复杂度"时触发。用变更放大、认知负荷、未知的未知三个维度，对指定功能/模块做代码级复杂度分析，输出散落点清单、隐含规则列表和隐蔽风险点。用于评审设计、评估技术债、判断改动风险、新功能开发前评估落点。
---

# 复杂度分析框架

用三个维度衡量一个功能的复杂度：变更放大、认知负荷、未知的未知。目标不是"代码行数多"，而是"一个设计决策散落到了多少地方"。

## 触发关键词

- `/complexity` — 启动标准分析流程
- `/complexity <功能名>` — 分析指定功能（如 `/complexity 家园锦鲤`）
- `/complexity <文件或目录>` — 分析指定代码范围
- "分析复杂度"、"复杂度分析"、"评估复杂度"、"这个功能复杂吗"

## 三维度定义

### 变更放大
一个逻辑变化，必须修改多少地方。核心问题：**一个设计决策散落到了太多地方**。
- 典型表现：同一份逻辑被复制（service 一份 + GM 一份）、一个概念分散在多层（proto/model/scene/flush）、红点/任务/日志/通知各有一处触发点。

### 认知负荷
正确使用代码前必须装进脑子的东西。核心问题：**代码短不代表简单**。
- 典型表现：字段名和实际语义不符（key=refresh_time 但值是生成时间戳）、必须先读另一个文件才能理解当前函数、满池跳过/沉默期起点等隐含规则无注释。

### 未知的未知
你不知道自己还需要知道什么。核心问题：**没有信号提示坑在哪里**。
- 典型表现：漏了投影函数（FlushXxx）编译不报错但前端收不到、硬编码值不跟随判定逻辑变化、副本复制时漏了一个记录点。

## 工作步骤

### 1. 确定分析目标
从用户输入或上下文提取功能名/文件范围。不确定时直接问用户。

### 2. 收敛范围
先读知识库（`doc/张天枭/kb_v2(聚焦scene和model)/README.md`），按需求类型选对应文档。不要直接全盘扫描 `go/src/app`。
- 快速路径：用户说"只分析这一个功能"时，用 `rg` 定位该功能的散落点即可，跳过知识库全文。

### 3. 定位散落点
搜索该功能的所有引用点（排除生成代码：`go/pkg`、`go/src/app/protocol`、`go/src/app/script`、`go/src/app/gm`）。区分核心实现文件和横切散落点。

### 4. 并行调研（推荐 spawn 2-4 个 explorer）

按功能复杂度分配：

| Agent | 调研维度 | 核心问题 |
|-------|---------|---------|
| A | 核心业务流程 | 协议入口、状态/类型、刷新/触发机制、领取/执行逻辑、隐含状态依赖 |
| B | 持久化层 | 用户 model vs 全局 model 分工、Save/Load/Decode/Flush 实现、字段一致性（存档 vs 前端 proto）、新增字段改动清单 |
| C | 配置层 | 配置表有哪些、怎么加载索引、type/group 等概念、策划改参数要改几处 |
| D | 横切散落点 | 红点(触发点+清除点)、任务、通知、日志、GM(操作清单)、重连恢复、功能开启 |

小功能可合并为 2 个 agent（核心+持久化 / 配置+散落点）。

### 5. 按三维度分析

#### 变更放大 — 扫这些信号
- [ ] 同一逻辑有没有复制副本？（service + GM 各写一遍 = 最典型）
- [ ] 一个概念散落在几层？（proto → model → scene → flush → 前端）
- [ ] 红点/任务/日志/通知各自有多少触发点？
- [ ] 改一个业务概念（如"奖励计算方式"），必碰几个文件？可能碰几个？
- [ ] 新增一个字段要穿几层？有没有"漏了不报错"的层？

#### 认知负荷 — 扫这些信号
- [ ] 字段名和实际语义是否一致？（key 叫 refresh_time 但值是生成时间戳）
- [ ] 有没有"必须先读另一个函数/文件才能理解当前代码"的依赖？
- [ ] 有没有无注释的隐含规则？（满池跳过、沉默期从初始赠送开始算）
- [ ] 存档字段和前端字段是否一致？两边字段号是否相同？
- [ ] 有没有运行时算出来但名字像存档字段的？（NextRefreshTime）
- [ ] 有没有废弃字段占位号段？新增字段是否需要避开？

#### 未知的未知 — 扫这些信号
- [ ] 有没有"漏了不报错"的地方？（FlushXxx 漏一个字段，编译通过前端收不到）
- [ ] 有没有硬编码值不跟随判定逻辑？（红点 `true` 不跟随 HasXxxRedPoint）
- [ ] 副本复制时有没有漏了某个记录点/校验点？
- [ ] 有没有 workaround 隐患？（拿奖励反推配置，改配置就失效）
- [ ] 有没有"改了 A 不知道要同步改 B"的情况？（改 service 不知道 GM 有副本）
- [ ] 有没有死代码/无关常量混在里面？（春节锦鲤常量混在家园锦鲤搜索结果里）

### 6. 汇总输出

## 输出格式

```
## 复杂度分析：<功能名>

### 一、变更放大
（逐条列出散落点，给文件:行号）

### 二、认知负荷
（逐条列出隐含规则，给文件:行号，标注"不读 X 不知道 Y"）

### 三、未知的未知
（逐条列出隐蔽风险，给文件:行号，标注"没有信号提示"）

### 总体评价
| 维度 | 严重度 | 一句话 |
（高/中高/中/低）

### 做得好的地方（Obvious 的部分）
（列出设计决策收归一处的部分，如持久化层干净、model 注册规范）

### 最值得修的 N 个问题
1. [问题] — [为什么] — [怎么改]
```

## 必守护栏

- 先用知识库和已有 skill 收敛范围，不要直接全盘扫描 go/src/app。
- 区分"调研相关范围"和"建议修改范围"——分析报告不改代码。
- 触及服务边界、存储语义、跨服规则时，明确写"待 owner 确认"。
- 每条结论给文件:行号，可回溯。
- 变更放大要量化："必碰 N 个文件，可能碰 M 个"。
- 未知的未知要解释"为什么没有信号"——是编译不报错、是接口没表达、还是代码结构看不出来。
- 认知负荷要解释"不读 X 就猜不到 Y"——不是"代码难读"，而是"隐含规则不可推断"。
- 做得好的地方也要写——好的设计决策值得标注，不只是挑毛病。
- 如果功能不大，三维度可以合并输出，不必硬凑结构。
